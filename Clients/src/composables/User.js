const User = {
    UserID: Number,
    UserName: String,
    UserProfilePic: String,
    UserLevel:   {
         default: 0,
        } ,
    UserExperience: {
        default: 0
    },
    
};
if (User === UserExits) {
    return `Please log in to your account`
} 

